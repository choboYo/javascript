let button = document.querySelector('button');
button.addEventListener('click', price);

function price() {
    document.querySelector('value');
    document.querySelector('price');
    let('value' + '*' + 'price.slice(-1)');
    let result = ('value' + '*' + 'price.slice(-1)');
    console.log(result)
}
